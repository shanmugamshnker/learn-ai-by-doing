# A2A Book Library Federation Demo
# =================================

# Web Framework & Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
starlette>=0.35.0

# HTTP Client (for A2A communication)
httpx>=0.26.0

# Data Validation
pydantic>=2.5.0

# =================================
# AI Frameworks (each agent uses different one)
# =================================

# OpenAI SDK (Central Portal Agent)
openai>=1.12.0
fasta2a>=0.6.0

# Pydantic AI (City Library Agent) - Native A2A support (needs a2a extras for FastA2A)
pydantic-ai[a2a,openai]>=0.0.14

# Strands Agents (University Library Agent) - Native A2A support
strands-agents[a2a,openai]>=0.1.0
strands-agents-tools>=0.1.0

# =================================
# Utilities
# =================================
python-dotenv>=1.0.0
typing-extensions>=4.9.0
